# 프라이버시 고지

> banker 플러그인의 업데이트-체크 서비스(업데이트 알림 + 익명 사용량 카운팅)에 대한 프라이버시 고지입니다.

## 개요

이 문서는 두 개의 독립된 기능을 설명합니다.\
하나는 새 버전이 나왔을 때 알려주는 **업데이트 알림**이고, 다른 하나는 유지보수자가 사용량을 파악하기 위한 **익명 사용량 카운팅**입니다.\
두 기능 모두 기본값은 켜짐(default-on)이며, 각각 독립적으로 끌 수 있습니다.\
이 기능은 Claude Code 런타임에서만 동작합니다. Codex CLI는 샌드박스가 네트워크 접근을 차단하므로 알림과 카운팅 둘 다 동작하지 않습니다.

## 현재 상태

버전 0.8.0은 아직 게시(publish)되지 않았습니다.\
공개는 아래 "법적 상태"에서 설명하는 검토를 통과한 뒤 진행합니다.\
이 문서의 문구는 그 검토 결과에 따라 더 정교해질 수 있습니다.

## 업데이트 알림

공개 npm 레지스트리(`https://registry.npmjs.org/@kaydash9999/banker-plugins/latest`)에서 최신 버전을 조회하고, 설치된 버전보다 최신 버전이 있으면 세션 시작 시 알립니다.\
이 조회 요청의 수신자는 **npm(GitHub/Microsoft가 운영)**입니다. banker 유지보수자는 이 조회를 전혀 수신하지 않습니다.\
update-notifier·Homebrew 등 다른 CLI 도구가 쓰는 것과 같은 방식입니다.

기본값은 켜짐입니다.\
환경변수 `BANKER_NO_UPDATE_CHECK` 를 설정하거나 config의 `updateCheck` 를 `false` 로 두면 끌 수 있습니다.

## 사용량 카운팅

하루 1회, 유지보수자가 운영하는 외부 엔드포인트(banker 패키지 밖에서 별도로 운영되는 serverless)로 익명 체크인을 전송합니다.\
플러그인은 기본 카운팅 엔드포인트(`banker.banker-plugins.workers.dev`)를 내장하므로, count-default-on 이라 설치 즉시 활성입니다.\
`BANKER_TELEMETRY_ENDPOINT` 환경변수로 자가호스팅 엔드포인트를 지정하거나 override 할 수 있습니다.

### 수집 항목

- 스킬/커맨드별 호출 횟수를 시간대(hour-of-day, 0시~23시)별로 버킷화한 값
- 플러그인 버전
- OS 종류(linux/darwin/win32 수준)

개별 호출의 시각은 전송하지 않고 시간대 버킷까지만 뭉쳐서 보냅니다.\
집계 대상은 사용자가 직접 입력한 `/banker:*` 커맨드와 모델이 자동으로 호출한 스킬 양쪽 모두이며, 서로 다른 두 훅이 겹치지 않게 나누어 포착합니다.

### 수집하지 않는 항목

- 이름, 이메일 등 개인식별정보(PII)
- 코드 및 파일 내용, 커맨드 인자
- 저장소·계정 정보
- 지속적인 사용자 또는 기기 식별자(UUID·머신ID 등을 생성하지 않습니다)
- 페이로드 안의 IP 주소

기본값은 켜짐(count-default-on)입니다.\
환경변수 `BANKER_NO_TELEMETRY` 를 설정하거나, config의 `telemetry` 를 `false` 로 두거나, `banker telemetry off` 를 실행하면 끌 수 있습니다.\
`banker setup` 첫 실행 시 이 기본값과 끄는 방법을 투명하게 고지합니다. 진행을 막는 opt-in 동의 프롬프트가 아니라 정보 제공입니다.

## 익명성에 대한 정직한 고지

익명성은 전송되는 데이터, 즉 페이로드 자체의 속성이지 시스템 전체의 속성이 아닙니다.\
카운팅 엔드포인트는 네트워크 요청의 시스템 경계에서 TCP 소스 IP 주소를 봅니다. IP 주소는 GDPR과 한국 개인정보보호법(PIPA) 모두에서 개인정보로 취급됩니다.\
이 엔드포인트는 banker 유지보수자가 운영하며, 수신 IP를 기록하지 않도록 설계되어 있습니다. banker 플러그인(클라이언트)과 카운팅 서버는 같은 운영자가 관리합니다.\
반면 업데이트 알림 조회의 IP는 npm만 보며, banker 유지보수자는 어느 경로로도 보지 않습니다.\
클라이언트인 banker가 보장하는 범위는 페이로드 안에 IP나 그 밖의 식별자를 넣지 않는다는 것까지이며, 서버가 수신 IP를 기록하지 않는 것은 서버를 운영하는 유지보수자의 설계 책임입니다.

## 상시 훅 비용

알림과 카운팅이 기본값으로 켜져 있기 때문에 다음 훅이 banker 활성화 중 항상 등록되어 있습니다.

- `SessionStart`: 세션 시작 시 1회, 캐시된 업데이트 알림을 확인합니다(이 훅 자체는 네트워크 요청을 하지 않습니다).
- `PostToolUse`(Skill 매처): 모델이 자동으로 호출한 스킬을 집계합니다.
- `UserPromptExpansion`: 사용자가 직접 입력한 `/banker:*` 커맨드를 집계합니다.

세 훅 모두 트리거될 때마다 짧게 실행되는 node 프로세스를 하나씩 기동합니다(수십 ms 수준).\
opt-out 하더라도 훅의 등록 자체는 남고, 각 스크립트 내부의 게이트가 즉시 무동작으로 반환합니다.

## 전송 안전

- 업데이트 알림 조회는 GET 요청이며 페이로드가 없습니다.
- 카운팅 체크인은 HTTPS만 사용합니다. 평문 http는 로컬 테스트용 loopback(localhost/127.0.0.1/::1) 엔드포인트에만 허용되고, 그 외 엔드포인트에는 거부됩니다.
- 네트워크 조회·전송은 세션을 막지 않도록 분리 실행되며, 포그라운드 훅은 캐시 읽기만 하고 즉시 반환합니다.
- 어떤 네트워크 오류도 삼키며(fail-closed), 항상 정상 종료합니다.

## 법적 상태

**EU ePrivacy는 양면을 그대로 남깁니다.**

표준 관행이라는 근거: 익명 체크인, 요청 카운트, IP 미보유, 사전 고지와 opt-out의 조합은 사실상 업계 표준 관행에 해당합니다. 지속 식별자나 사용자별 프로파일을 만들지 않는 무상태 체크인이라는 점이 이 판단의 근거입니다.

반대 근거: EU ePrivacy Directive Art.5(3)은 개인식별정보 여부와 무관하게 단말기(device) 접근 자체에 이용자 동의를 요구할 수 있습니다. Planet49 판례에 따르면 분석 목적의 opt-out은 유효한 동의로 인정되지 않을 수 있습니다. 즉 무상태 체크인이라도 접근 자체가 EU에서는 동의를 유발할 가능성이 있습니다.

실무 결론: 실제로 EU 사용자를 대상으로 배포하기 전에는 opt-in 전환 여부를 별도로 검토해야 합니다. 게시(publish)는 이 검토를 포함한 별도의 법률 게이트이며, 아직 통과하지 않았습니다.

**미국 CCPA·한국 PIPA는 이 익명 업데이트-체크에 재적용하지 않습니다.** 두 법의 실무 판단 기준은 IP 주소의 지속적 보유, 사용자별 프로파일 구축, 데이터 판매 여부입니다.\
이 기능의 익명 체크인은 이 중 어느 것도 하지 않습니다. IP를 기록하지 않고, 지속 식별자가 없고, 데이터를 판매하지 않습니다.\
따라서 CCPA·PIPA를 이 기능의 별도 미해결 법적 쟁점으로 표기하지 않습니다. 위에서 다룬 EU ePrivacy는 이와 무관한 별건입니다.

이 문구는 향후 더 정교해질 수 있습니다.
