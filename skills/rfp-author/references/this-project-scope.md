# 프로젝트 과업내용 / 산출물 — 기술요구 골격 (빈 템플릿)

> **이 파일은 프로젝트별로 채우는 빈 템플릿이다.** 대상 시스템의 코드/spec/디자인 SoT를 근거로 각 섹션의 `<채울 값>`을 메운다.
> 작성 규칙: 모든 수치·구성은 근거(`file:LINE`, 디자인 `<DesignRef(page-section)>`, 인터뷰 등)를 동반(verify-before-claim). 요건 발의형(기본)이면 현황(As-Is)·실호스트/포트·내부식별자는 **벤더 발송본 비노출**, 기획(planned)만 요건으로 공개 — [vendor-rfp-structure.md](vendor-rfp-structure.md) 노출 게이트 준수.
> 요구사항 ID 체계(SFR/PER/ECR/INR/DAR/TER/SER/QUR/COR/PMR/PSR/MPR/MHR)는 [korean-rfp-framework.md](korean-rfp-framework.md) §1.

## §1. 과업 개요
- 사업명: `<채울 값>`
- 목적/배경: `<채울 값 — 왜 구축/외주하는가, 해결할 문제>`
- 과업 범위 한 줄 요약: `<채울 값 — 예: FE + BE + (AI 에이전트) + 데이터 + 외부연계 N종>`
- 외주 모드: `<요건 발의형 | 동일재구현>` (동일재구현이면 스택을 COR로 고정)

## §2. 대상 시스템 개요 (ECR — 시스템장비구성)
시스템 계층·구성·외부연계를 표로. (벤더 발송본에는 실제 호스트/포트 대신 논리 구성·요건만)

| 계층 | 구성 | 비고 |
|---|---|---|
| FE | `<프레임워크/배포 형태>` | `<채울 값>` |
| BE | `<런타임/프레임워크/패키징>` | `<채울 값>` |
| (AI/LLM) | `<오케스트레이터/모델 제공방식>` | `<해당 시>` |
| 데이터 | `<DBMS/스키마 형태>` | `<채울 값>` |
| 캐시/큐 | `<해당 시>` | `<채울 값>` |
| 외부 연계 | `<외부 시스템 N종 — 보안필터/도메인 API/검색 등 역할만>` | `<채울 값>` |

## §3. 기능 요구 (SFR — 기능 요구사항)
- 핵심 기능 1: `<채울 값 — 기능명 + testable 정의 + 산출 화면/API>`
- 핵심 기능 2: `<채울 값>`
- 화면/카드/위젯 규모: `<채울 값 — §물량과 연동, 근거 page/file 동반>`
- (AI 에이전트가 있으면) 질의/응답 스키마·렌더 방식: `<채울 값>`

## §4. 기술 요구 / 제약 (COR / ECR)
동일재구현이면 스택은 **선택이 아닌 제약**; 요건 발의형이면 **호환 요건 + 대안 협의**로 기술.
- BE 스택·금지사항(예: ORM/마이그레이션 도구/특정 패턴 금지 여부): `<채울 값>`
- FE 스택·금지사항(예: SSR 금지 여부, 상태/쿼리 라이브러리): `<채울 값>`
- AI/LLM 스택(런타임·SDK·provider 추상화): `<해당 시>`
- 버전 핀: `<채울 값 — exact 핀이 필요한 항목을 COR로 명시>`
- 권위 출처: `<spec file:LINE>`

## §5. 비기능 요구 (PER / DAR / INR / TER / QUR)
- **PER(성능)**: `<채울 값 — 캐시 전략, 동시성 상한, latency 목표>`
- **DAR(데이터)**: `<채울 값 — 데이터 모델, 마스터 규모, 적재/리시드 파이프라인>`
- **INR(인터페이스)**: `<채울 값 — 외부연계 계약·인증·타임아웃·TLS 정책>`
- **TER(테스트)**: `<채울 값 — 골든 검증(빌드/유닛/통합), 시각회귀, e2e>`
- **QUR(품질)**: `<채울 값 — SoT 1:1 정합, (LLM이면) 사실정확도/grounding 임계>`

## §6. 비기능 요구 — 보안 / 접근성 (SER / QUR)
- **SER(보안)**: `<채울 값 — 입출력 검증/fail 정책, (LLM이면) prompt injection direct+indirect 방어, CVE/SBOM, 시크릿 비노출>`. 규제 상세 → [compliance-and-ai-llm.md](compliance-and-ai-llm.md).
- **접근성**: `<채울 값 — 준수 기준(예: KWCAG/WCAG 레벨)>`
- 〈OPEN〉 보안 항목(망분리/PIPA/보안성심의/secure-coding 등 조문 확정 필요)은 [research-provenance.md](research-provenance.md) 참조 후 SER에 반영.

## §7. 제약·가정 (COR)
- 전제/가정: `<채울 값 — 발주자 제공 자료, 환경 제약, 외부 의존>`
- 제외 범위(Out of Scope): `<채울 값 — 명시적으로 과업에서 뺄 항목>`

## §8. 산출물 목록 + SoT 전달 (PSR / PMR)
- **SoT 자산(원본자료)**: `<채울 값 — spec, 디자인 PDF/이미지, 인벤토리/카탈로그, 지식문서>`
- **인도물(예)**: 소스 · DB 스키마/DDL · 골든 테스트 · 빌드/배포 가이드 · 운영 문서 · API 명세(OpenAPI) · OSS 라이선스 보고서.
- ★ **OPEN(주의)**: SoT를 과업내용·산출물로 전달하되 **기밀 내부값(시크릿/실데이터/외부 토큰) 누출 없이** 전달하는 방식(격리망/마스킹/NDA)은 프로젝트별 확정 — [research-provenance.md](research-provenance.md) open question.

## §9. 일정·마일스톤 (PMR) / 배포·운영 환경 (ECR)
- **마일스톤**: `<채울 값 — 빌드 순서에 정렬한 단계별 산출물↔지급. 예: 데이터/BE 계약 → FE 렌더 → AI/보안 오케스트레이션 → 통합검수/이관>`
- **운영 타깃 환경**: `<채울 값 — 웹서버/WAS/프로토콜 버전 등, ECR로 명시>`
- **dev 환경 divergence**: `<해당 시 — 운영과 다른 dev 간이구성은 참고로만 표기, 요건 아님>`
